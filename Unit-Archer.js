// JavaScript source code

class UnitArcher extends Unit {
    constructor(scene, autotile, config) {
        config = config || {};
        config.name = config.name || name;
        super(scene, autotile, config);
    }
}

IMAGE_INSERT('image', `unit-archer`);